country_event = {
	id = 2341
	title = "Trafer The $FROMCOUNTRY$"
	desc = "Trafer The $FROMCOUNTRY$"

    is_triggered_only = yes

	option = {
		name = "Accept Trafer"
		any_owned = {
			limit = {
				has_building = province_selector
			}
			change_controller = PRG
			secede_province = PRG
		}
		any_owned = {
			limit = {
				has_building = province_selector
			}
			province_selector = -1
		}
		FROM = {
    add_country_modifier = {
        name = trafer_tomado
        duration = 1
    }
}
		any_country = {
			limit = {
				has_country_modifier = trafer_tomado	
			}
		inherit = PRG
		}
		any_country = {
		add_country_modifier = {
		    name = trafer_tomadoo
			duration = 60
		}
		}
		
		any_country = {
			limit = {
				has_country_modifier = trafer_tomado
			}
		remove_country_modifier = trafer_tomado
		}
		ai_chance = {
			factor = 0
		}
		}

	option = {
		name = "Decline Trafer"
		any_country = {
			limit = {
				has_country_modifier = trafer_tomado
			}
		remove_country_modifier = trafer_tomado
		}
		any_owned = {
			limit = {
				has_building = province_selector
			}
			province_selector = -1
		}
			clr_country_flag = trafer_tomado
		ai_chance = {
			factor = 1
		}
	}
}